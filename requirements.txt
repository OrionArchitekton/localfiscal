# Runtime dependencies (the package also declares these in pyproject.toml).
typer>=0.9
fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9
pypdf>=4.0
Pillow>=10.0
fpdf2>=2.7
# Optional local vision uses the standard library (urllib) — no extra dependency.
# It is OFF unless you set OLLAMA_URL / pass --vision and run Ollama yourself.
